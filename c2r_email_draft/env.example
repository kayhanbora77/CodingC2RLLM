# ── Email credentials ──────────────────────────────────────────
EMAIL_ADDRESS=kayhanboraisteps@gmail.com
EMAIL_PASSWORD=ikamefbnoqtqtwnq        # Gmail: use App Password, NOT real password
IMAP_SERVER=imap.gmail.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# ── RAG / LLM settings ─────────────────────────────────────────
OLLAMA_MODEL=tinyllama          # options: tinyllama | phi | gemma:2b
                                # tinyllama = 637MB, best for 12GB RAM
EMBEDDING_MODEL=all-MiniLM-L6-v2  # 80MB, very fast on CPU
FAQ_CSV_PATH=click2refund_faq.csv

# ── Auto-reply behaviour ───────────────────────────────────────
CHECK_INTERVAL_SECONDS=60       # how often to poll inbox
AUTO_SEND=false                 # set true to send without confirmation
CONFIDENCE_THRESHOLD=0.45       # min cosine similarity to use FAQ answer
